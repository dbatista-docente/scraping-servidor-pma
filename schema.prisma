// schema.prisma
generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:./database.db"
}

model EmailLog {
    id    String  @id @default(uuid())
    month String  @unique
    sent  Boolean
}
